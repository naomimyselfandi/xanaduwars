package io.github.naomimyselfandi.xanaduwars.ext;

/// A singleton enum used as a null-safe replacement for [Void].
public enum None {

    /// A singleton enum used as a null-safe replacement for [Void].
    NONE

}
